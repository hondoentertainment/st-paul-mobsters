# Public site URL for Open Graph and canonical links (no trailing slash).
# VITE_SITE_URL=https://your-domain.example

# Set to 1 if the Vite dev server on Windows/OneDrive/network folders misses HMR or feels stuck.
# VITE_WATCH_POLLING=1
